"""
Parser module — kept for backward compatibility but no longer contains model parsers.

NeuroScope is now a visual deep learning builder. Model parsing has been removed.
The backend generates code from user configurations rather than parsing uploaded models.
"""
